## 📝 문제 요약

알파벳이 적혀져있는 보드판에서 좌측 상단 부터 이동을 한다.

단, 지나온 칸에 적혀있던 알파벳은 다시 지나갈 수 없을때 최대 몇칸을 지날수 있는지?

## 💡 접근 방법

- **알고리즘/자료구조:** 백트래킹
- **핵심 아이디어:**
    - 재귀함수를 이용하여 현재 위치에서 4방향으로 탐색
    - 이미 방문했으면 return;
    - 방문하지 않았다면 방문표시하고 다시 max 값 업데이트 → 탐색
    - 다시 visited 값 돌려주기

## ✅ 풀이 과정

- **문제 분석**
    - 같은 알파벳을 두 번 방문할 수 없으므로, 현재 경로에서 사용한 알파벳을 관리해야 한다.
    - 모든 가능한 경로를 탐색해야 하므로 DFS가 적합하다.
- **해결 전략 수립**
    - DFS를 이용해 한 방향으로 끝까지 이동한 뒤, 되돌아오며 다른 방향을 탐색한다.
    - 알파벳은 총 26개이므로 `boolean[26]` 배열을 사용해 방문 여부를 관리한다.
- **코드 구현**
    - `(0,0)`에서 DFS를 시작한다.
    - 현재 칸의 알파벳을 방문 처리한 후, 네 방향으로 이동하며 재귀 호출한다.
    - 재귀 호출이 끝나면 방문 처리를 해제하여 백트래킹을 수행한다.
- **테스트 및 검증**
    - 작은 입력부터 최대 크기의 입력까지 테스트하여 정상적으로 최대 이동 칸 수가 계산되는지 확인한다.