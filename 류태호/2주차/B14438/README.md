### 📌 문제 정보

- **번호:** 14438
- **제목:** 수열과 쿼리 17
- **난이도:** Gold 1
- **분류:** 세그먼트 트리, 자료구조

---

### 💡 접근 방식

> 구간 최솟값 질의와 값 변경이 최대 100,000번씩 주어지므로, 매 쿼리마다 구간을 순회하면 시간 초과가 발생합니다.  
> 따라서 **세그먼트 트리**를 이용해 구간 최솟값과 점 업데이트를 모두 `O(log N)`에 처리했습니다.

#### 1단계 - 세그먼트 트리 빌드
- 루트 노드가 `[1..N]` 구간을 담당하도록 시작합니다.
- 구간을 `mid` 기준으로 `[start..mid]`, `[mid+1..end]`로 나누며 재귀적으로 트리를 구성합니다.
- 각 노드에는 담당 구간의 **최솟값**을 저장합니다.

#### 2단계 - 업데이트(1 i v)
- 인덱스 `i`가 포함된 구간 노드들만 따라 내려가 리프를 갱신합니다.
- 올라오면서 부모 노드의 최솟값을 다시 계산합니다.

#### 3단계 - 쿼리(2 i j)
- 질의 구간과 노드 구간이
  - 겹치지 않으면 무시(INF 반환)
  - 완전히 포함되면 해당 노드 값 반환
  - 일부만 겹치면 자식으로 내려가 결과를 합침(min)
- 이렇게 구간 최솟값을 `O(log N)`에 구합니다.

---

### 💻 핵심 코드

#### build (초기 트리 구성)

```java
static void build(int node, int start, int end, int[] input) {
    if (start == end) {
        seg[node] = input[start];
        return;
    }
    int mid = (start + end) / 2;
    build(node * 2, start, mid, input);
    build(node * 2 + 1, mid + 1, end, input);
    seg[node] = Math.min(seg[node * 2], seg[node * 2 + 1]);
}
```

#### update (점 업데이트)

```java
static void update(int node, int start, int end, int idx, int val) {
    if (idx < start || idx > end) return;

    if (start == end) {
        seg[node] = val;
        return;
    }

    int mid = (start + end) / 2;
    update(node * 2, start, mid, idx, val);
    update(node * 2 + 1, mid + 1, end, idx, val);
    seg[node] = Math.min(seg[node * 2], seg[node * 2 + 1]);
}
```

#### query (구간 최솟값)

```java
static int query(int node, int start, int end, int l, int r) {
    if (r < start || end < l) return Integer.MAX_VALUE;

    if (l <= start && end <= r) return seg[node];

    int mid = (start + end) / 2;
    return Math.min(
        query(node * 2, start, mid, l, r),
        query(node * 2 + 1, mid + 1, end, l, r)
    );
}
```

---

### ⏳ 복잡도 계산

- **시간 복잡도:** `O((N + M) log N)`
    - build: `O(N)`
    - update/query: 각 `O(log N)` × M

- **공간 복잡도:** `O(N)`
    - 세그먼트 트리 배열: `seg[4 * N]`

---

### ⚠️ 어려웠던 점

- 초기에는 단순 배열로 구하려 했지만, 시간 초과가 발생했습니다.
- 이에 검색해보며 세그먼트 트리를 알았고, 동작 방식과 포함/부분 겹침 조건을 이해하는 데 시간이 걸렸습니다.
- 특히 트리 배열 크기를 왜 `4 * N`으로 잡는지 몰랐는데, N이 2의 거듭제곱이 아닐 때도 인덱스가 안전하게 동작하도록 관례적으로 `4 * N`을 사용한다는 점을 새롭게 알게 되었습니다.


---

### 🆕 새롭게 알게 된 것

- 세그먼트 트리는 구간을 인덱스 기준으로 반씩 나누고, 각 노드에 구간의 결과(여기서는 최소값)를 저장한다는 점
- query에서 "완전 포함"이면 바로 반환해도 되는 이유는 해당 구간의 최소값이 이미 노드에 저장되어 있기 때문이라는 점
- 업데이트는 바뀐 인덱스가 포함된 경로만 내려가서 갱신하고, 올라오면서 부모 값을 재계산한다는 점

---

### 📌 언제 사용하는가

- **점 업데이트**와 **구간 질의(최소/최대/합 등)**가 함께 존재하는 문제
- 쿼리 개수가 많아서 매번 구간을 순회하면 시간 초과가 나는 문제
- `O(N)` 구간 연산을 `O(log N)`으로 줄여야 하는 경우