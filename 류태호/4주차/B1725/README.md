### 📌 문제 정보

- **번호:** 1725  
- **제목:** 히스토그램  
- **난이도:** Platinum 5  
- **분류:** 분할 정복, 스택(대표 풀이), 구현

---

### 💡 접근 방식

> 히스토그램에서 만들 수 있는 가장 큰 직사각형 넓이를 구하는 문제로, 분할 정복 방식으로 해결했습니다.  
> 구간을 반으로 나누어 왼쪽/오른쪽 구간의 최대 넓이와, 가운데를 걸치는 최대 넓이를 계산해 전체 최댓값을 갱신합니다.

---

### 🔹 1단계 – 구간 분할

- `solve(left, right)`로 현재 구간의 최대 넓이를 계산
- `mid`를 기준으로
  - 왼쪽 구간 최대 넓이: `solve(left, mid)`
  - 오른쪽 구간 최대 넓이: `solve(mid+1, right)`
- 큰 값을 갖고감

---

### 🔹 2단계 – 가운데를 포함하는 최대 직사각형 계산

- 가운데를 걸치는 경우도 고려해야 함
- 시작은 `leftIdx = mid`, `rightIdx = mid+1`로 두 칸을 포함하는 구간에서 시작
- 현재 구간의 높이는 `height = min(arr[leftIdx], arr[rightIdx])`로 둘 중 작은 값으로 시작
- 좌/우로 한 칸씩 확장해가며 더 큰 높이를 유지할 수 있는 방향(높은 쪽)을 우선 확장
- 확장할 때마다 `height`를 갱신하고 `height * width`로 넓이를 갱신

---

### 💻 핵심 코드 (가운데 확장 부분)

```java
int leftIdx = mid;
int rightIdx = mid + 1;
int height = Math.min(arr[leftIdx], arr[rightIdx]);
area = Math.max(area, height * 2);

while (leftIdx > left || rightIdx < right) {
    if (rightIdx < right) {
        if (leftIdx == left) {
            rightIdx++;
            height = Math.min(height, arr[rightIdx]);
        } else if (arr[leftIdx - 1] < arr[rightIdx + 1]) {
            rightIdx++;
            height = Math.min(height, arr[rightIdx]);
        } else {
            leftIdx--;
            height = Math.min(height, arr[leftIdx]);
        }
    } else {
        leftIdx--;
        height = Math.min(height, arr[leftIdx]);
    }
    area = Math.max(area, height * (rightIdx - leftIdx + 1));
}
```

---

### ⏳ 복잡도 분석

- **시간 복잡도:** O(N log N)
    - 분할 정복으로 구간을 나누고,
    - 각 구간에서 가운데 확장은 전체적으로 O(N) 수준으로 수행되어 log 단계와 결합됨

- **공간 복잡도:** O(log N)
    - 재귀 호출 스택

---

### ⚠️ 어려웠던 점
- 비슷한 문제(히스토그램에서 가장 큰 직사각형 B6549)를 풀어봐서 구현할 수 있었습니다.
---