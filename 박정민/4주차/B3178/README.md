# BOJ 3178 - 코코스

## 📌 문제 설명

2K개의 글자로 이루어진 단어 N개가 주어진다.

코코스는 글자 하나를 포함하고 있는 정점으로 이루어진 유향 그래프(Directed graph)를 말한다. 이때, 모든 단어는 그래프 상의 경로가 존재해야 하며, 이 경로에 쓰여 있는 글자를 순서대로 적었을 때 단어와 일치해야 한다.

또한, 경로 상의 모든 정점은 아래와 같은 네 가지 조건을 만족해야 한다.

1. 첫 정점의 in-degree는 0이다.
2. 다음 K-1개 정점의 in-degree는 1이다.
3. 다음 K-1개 정점의 out-degree는 1이다.
4. 마지막 정점의 out-degree는 0이다.
   위의 조건은 경로는 첫 K개 글자에서 나누어질 수 있고, 마지막 K개 글자에서 만날 수 있음을 의미한다.

단어 N개가 주어진다. 이때, 정점의 수가 가장 적은 코코스를 구하는 프로그램을 작성하시오.

## 💡 해결 아이디어

1. 앞에 k개는 out-degree에 제약이 없고 뒤에 k개는 in-degree에 제약이 없네. 앞부터 일치하는대로 따라가다가 일치하지 않는 순간부터 빠져나오고 뒤엔 반대로 하면 되겠다.
2. 단어를 반으로 잘라서 앞뒤를 따로 관리하는게 편하겠네.

## 🧠 코드 해설

```java
public static class Node {
		char value;
		LinkedList<Node> next;

		public Node(char value) {
			this.value = value;
			next = new LinkedList<>();
		}

		public void addNext(Node n) {
			next.add(n);
		}

		public boolean contains(char value) {
			if (this.value == value) return true;
			return false;
		}

		public LinkedList<Node> getNext() {
			return next;
		}
	}
```

vertex를 나타낼 class Node

```java
LinkedList<Node> first = new LinkedList<>();
LinkedList<Node> last = new LinkedList<>();
```

단어를 반을 쪼개서 앞과 뒤를 따로 관리하기

```java
        for (Node v : vertex) {
					if (v.contains(input[j])) {
						vertex = v.getNext();
						find = true;
						break;
					}
				}
```

탐색중인 vertex 중에 찾는 값이 있을 경우 다음으로 넘어가기

```java
				count += k-j;
				Node next = new Node(input[j]);
				vertex.add(next);

				for (int l = j+1; l < k; l++) {
					Node tmp = new Node(input[l]);
					next.addNext(tmp);
					next = tmp;
				}
```

탐색중인 vertex 중에 찾는 값이 없을 경우 vertex에 그 값을 추가하고 나머지들도 추가해서 연결해주기

## 🚀 느낀점

문제 설명이 좀 난해해서 이해하는데 시간을 좀 쓴거 같다. 예시 이미지가 첫번째 예제 이미지인줄 알고 이미지 이해를 아예 못했는데 알고보니 3번째 예제여서 좀 당황스러웠다..  
처음에는 set에 단어를 추가하는 식으로 했는데(abcdef일 경우 a, ab, abc를 넣는 식) 메모리 초과가 나서 처음부터 다시 했다. 이제와서 생각해보면 나올 수 있는 조합이 26^k라 너무 많은거 같긴 하다.
