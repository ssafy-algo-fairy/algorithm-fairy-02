# 🧩 BOJ 18808 - 스티커 붙이기

- 🔗 문제 링크: https://www.acmicpc.net/problem/18808
- 🏷️ 분류: 구현, 브루트포스(시뮬레이션), 회전

---

## 📌 문제 요약
크기 `N×M` 노트북에 `K`개의 스티커를 순서대로 붙인다.

- 각 스티커는 `x×y` 크기의 0/1 격자이며, `1`인 칸이 실제 스티커 영역이다.
- 스티커는 **회전(90도)** 할 수 있으며 최대 4방향(0°, 90°, 180°, 270°)을 시도한다.
- 스티커의 `1` 칸이 노트북의 이미 붙은 `1` 칸과 겹치면 붙일 수 없다.
- 가능한 위치가 있으면 **가장 먼저 찾은 위치**에 붙이고 다음 스티커로 넘어간다.
- 끝까지 붙인 후 노트북에서 `1`의 개수를 출력한다.

---

## 💡 풀이 아이디어
각 스티커에 대해 다음을 순서대로 수행한다.

1) 현재 방향(회전 상태)에서  
   노트북의 가능한 모든 좌표 `(x, y)`에 대해 붙일 수 있는지 검사한다.
2) 붙일 수 있으면 즉시 붙이고 다음 스티커로 이동한다.
3) 모두 실패하면 스티커를 90도 회전하고 다시 1)부터 시도한다.
4) 총 4번 회전해도 못 붙이면 그 스티커는 버린다.

즉, **(4회 회전) × (모든 위치 탐색) × (겹침 검사)** 를 구현한 시뮬레이션이다.

---

## ✅ 구현 구성
### 1) 노트북 상태
- `notebook[N][M]`
  - `1`이면 이미 스티커가 붙은 칸

### 2) 스티커 구조체
- `Sticker(x, y, st[x][y])`
  - `x, y` : 현재 스티커의 행/열 크기
  - `st` : 스티커 0/1 배열

---

## 🔍 붙일 수 있는지 확인 + 붙이기: `able(x, y)`
현재 스티커를 노트북 좌표 `(x, y)`에 올린다고 가정한다.

1) **겹침 검사**
- 스티커의 모든 칸 `(i, j)`에 대해
  - `st[i][j] == 1` 이면서 `notebook[x+i][y+j] == 1`이면 실패

2) **가능하면 실제로 붙이기**
- 겹침이 없으면 스티커의 `1`인 칸을 노트북에 반영
  - `notebook[x+i][y+j] = 1`

반환값:
- 붙였으면 `true`, 아니면 `false`

---

## 🔄 스티커 회전(90도): `rotation()`
스티커를 시계 방향 90도로 회전한 새 스티커를 만든다.

- 회전 전: `x × y`
- 회전 후: `y × x`

인덱스 변환(코드 기준):
- `new[i][j] = old[x - 1 - j][i]`

회전 결과를 `sticker = rotation()`으로 갱신해 다음 시도에 사용한다.

---

## 🔁 전체 흐름(스티커 1개 기준)
- `rot = 0..3` (최대 4번 회전)
  - `x = 0..N-sticker.x`
    - `y = 0..M-sticker.y`
      - `able(x, y)`가 true면 즉시 종료(해당 스티커 붙이기 완료)
  - 못 붙였으면 회전 후 다시 탐색

---

## ✅ 정답 계산
모든 스티커 처리가 끝난 뒤,
`notebook` 전체에서 값이 `1`인 칸의 개수를 세어 출력한다.

---

## ⏱️ 복잡도
스티커 1개에 대해:
- 회전 최대 4회
- 위치 탐색: `O((N - x + 1)(M - y + 1))` (대략 `O(NM)`)
- 붙임 가능 검사: 스티커 크기 `O(xy)`

따라서 대략:
- 시간 복잡도: `O(K * 4 * N * M * (스티커크기))`
- 공간 복잡도: `O(NM + 스티커크기)`

(문제 제한이 크지 않아 구현/브루트포스로 충분히 통과 가능한 형태)

---

## 📝 구현 포인트(코드 기준)
- “붙일 수 있는지 검사”와 “실제로 붙이기”를 `able()`에서 함께 처리해 흐름이 단순함
- 회전할 때 새 배열을 만들어 크기 변화를 자연스럽게 처리함
- 각 스티커는 “처음 붙는 위치에 바로 붙인다”는 문제 조건을 그대로 구현함

---
