# 🧮 BOJ 19591 - 독특한 계산기

- 🔗 문제 링크: https://www.acmicpc.net/problem/19591
- 🏷️ 분류: 구현, 투 포인터(양끝 처리), 연산 우선순위/그리디 시뮬레이션

---

## 📌 문제 요약
하나의 수식 문자열이 주어진다. (숫자와 연산자 `+ - * /`로 구성)

이 계산기는 일반적인 괄호/우선순위만 따르는 것이 아니라,
**수식의 양 끝에서 하나의 연산을 선택해 계산하고, 그 결과로 수식을 줄이는 과정을 반복**한다.

선택 규칙(핵심):
- 양 끝에서 가능한 연산 두 개를 후보로 두고,
- `*`/`/`가 `+`/`-`보다 우선
- 우선순위가 같으면 실제로 계산했을 때 결과가 더 큰 쪽을 선택
- 위 과정을 연산자가 1개 남을 때까지 반복 후 최종 값을 출력

---

## 💡 풀이 아이디어
수식을 통째로 파싱해서 트리로 만드는 방식이 아니라,
문제 규칙이 “양끝에서만 계산”이므로 **양끝 숫자와 연산자를 뽑아가며 시뮬레이션**한다.

구현 전략:
1) 문자열에서 맨 앞 숫자(`frontNum`)와 맨 뒤 숫자(`backNum`)를 먼저 파싱
2) 남은 수식에서
   - 앞쪽 후보 연산: `frontNum (o1) n1`
   - 뒤쪽 후보 연산: `n2 (o2) backNum`
   두 개를 동시에 준비
3) 규칙에 따라 둘 중 하나를 선택해 계산 결과로 `frontNum` 또는 `backNum`을 갱신
4) 포인터를 되돌리거나 진행시키면서(선택하지 않은 쪽은 유지) 연산자 개수를 1 줄임
5) 연산자가 1개 남으면 `frontNum (op) backNum`을 계산해 출력

---

## ✅ 구현 포인트 1: 투 포인터로 양끝 파싱
- `start`: 앞쪽에서 읽는 포인터
- `end`: 뒤쪽에서 읽는 포인터

### 앞 숫자 파싱
- 첫 글자가 `-`면 첫 숫자가 음수인지(`isMinus`) 처리
- 연산자가 나오기 전까지 자릿수를 누적해서 `frontNum` 생성

### 뒤 숫자 파싱
- 뒤에서부터 연산자가 나올 때까지 숫자를 읽어 `backNum` 생성

연산자가 아예 없는 경우(`operCnt == 0`)는 숫자만 출력하도록 예외 처리한다.

---

## ✅ 구현 포인트 2: 두 후보 연산을 만들어 비교 (`operCheck`)
반복 루프에서 항상 다음 두 연산을 준비한다.

- 앞 후보:
  - 연산자 `o1 = cArr[start + 1]`
  - 그 뒤 숫자 `n1` 파싱
  - 결과 `t1 = operation(frontNum, n1, o1)`

- 뒤 후보:
  - 연산자 `o2 = cArr[end - 1]`
  - 그 앞 숫자 `n2` 파싱
  - 결과 `t2 = operation(n2, backNum, o2)`

### 선택 규칙 구현
1) 우선순위 비교
- 앞이 `*`/`/`이고 뒤가 `+`/`-` → 앞 선택
- 앞이 `+`/`-`이고 뒤가 `*`/`/` → 뒤 선택

2) 우선순위가 같으면 결과 비교
- `t1 >= t2`이면 앞 선택, 아니면 뒤 선택

---

## ✅ 구현 포인트 3: 선택에 따른 상태 갱신
- 앞 연산을 선택하면:
  - `frontNum = t1`
  - 뒤쪽에서 미리 파싱하며 당겨둔 포인터(`end`)는 원래대로 되돌림 (`end = p2`)

- 뒤 연산을 선택하면:
  - `backNum = t2`
  - 앞쪽 포인터(`start`)를 원래대로 되돌림 (`start = p1`)

이렇게 하면 “선택하지 않은 쪽 후보”는 다음 반복에서도 그대로 유지될 수 있다.

---

## ⏱️ 복잡도
- 문자열을 양쪽에서 한 번씩 훑으며 숫자/연산자를 처리
- 각 연산자당 상수 작업 수행

- 시간 복잡도: `O(L)` (L = 문자열 길이)
- 공간 복잡도: `O(1)` (추가 배열 없이 포인터로 처리)

---

## 📝 주의사항(코드 기준)
- 첫 숫자가 음수인 케이스를 별도로 처리(`isMinus`)
- 나눗셈은 Java의 정수 나눗셈 규칙을 따른다(`long` / `long`)
- 뒤에서 숫자를 파싱할 때 `Math.pow`를 사용해 자릿값을 더하는 방식으로 구현되어 있음

---