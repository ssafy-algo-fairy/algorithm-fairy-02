# [BJ 1826] 연료 채우기 - Java

### 📋 문제 개요
- **알고리즘 유형**: 그리디 알고리즘 (Greedy), 자료구조 (우선순위 큐)
- **문제 핵심**: 시작점에서 목적지까지 이동하는 동안 최소한의 횟수로 주유소를 방문해야 합니다. 현재 보유한 연료로 갈 수 있는 주유소들을 모두 파악한 뒤, 그중에서 **가장 많은 연료를 보충할 수 있는 곳을 우선적으로 선택(Greedy)**하여 이동 거리를 갱신하는 것이 핵심입니다.

---

### 💡 풀이 로직
1. **데이터 구조**: 
   - `Node[] gasStation`: 각 주유소의 위치(`start`)와 보충할 수 있는 연료량(`amount`)을 저장합니다.
   - `PriorityQueue<Integer> pq`: 현재 도달 가능한 주유소들의 연료량을 **내림차순(최대 힙)**으로 보관합니다.
2. **초기 정렬**: 
   - 거리가 가까운 주유소부터 순차적으로 방문할 수 있도록, 주유소 배열을 시작점으로부터의 거리 오름차순으로 정렬합니다. (거리가 같다면 연료량이 많은 순으로 정렬)
3. **그리디 + 우선순위 큐 탐색**: 
   - 현재 연료(`P`)는 곧 현재 갈 수 있는 '최대 거리'를 의미합니다. 목적지(`L`)에 도달할 때까지 다음을 반복합니다.
   - 현재 거리(`P`) 이내에 있는 모든 주유소의 연료량을 우선순위 큐에 넣습니다.
   - 큐에서 가장 큰 연료량을 꺼내 현재 연료(`P`)에 더해주고, 방문 횟수(`answer`)를 1 증가시킵니다.
4. **예외 처리 (도달 불가)**: 
   - 아직 목적지에 도달하지 못했는데 우선순위 큐가 비어있다면, 더 이상 앞으로 나아갈 수 없음을 의미하므로 `-1`을 반환합니다.

---

### 💻 코드 주요 부분 설명
```java
static int solve(){
    // 도달 가능한 주유소의 연료량을 저장할 최대 힙 (가장 많은 연료 우선)
    PriorityQueue<Integer> pq = new PriorityQueue<>((s1, s2) -> Integer.compare(s2, s1));
    int answer = 0; // 주유소 방문 횟수
    int idx = 0;    // 탐색할 주유소 인덱스
    
    // 현재 갈 수 있는 최대 거리(P)가 목적지(L)에 도달하기 전까지 반복
    while(P < L){
        // 1. 현재 연료(P)로 갈 수 있는 모든 주유소의 연료량을 큐에 추가
        while(idx < N && gasStation[idx].start <= P){
            pq.add(gasStation[idx].amount);
            idx++;
        }
        
        // 2. 갈 수 있는 주유소가 없는데 목적지에 도착하지 못한 경우 예외 처리
        if(pq.isEmpty()) return -1;

        // 3. 도달 가능한 주유소 중 가장 많은 연료를 보충하여 이동 가능 거리 연장
        P += pq.poll();
        answer++;
    }

    return answer; // 최소 주유 횟수 반환
}
```